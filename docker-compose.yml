services:
  mongo:
    image: mongo:7
    container_name: ui3-mern-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - ui3_mern_mongo:/data/db

volumes:
  ui3_mern_mongo:
